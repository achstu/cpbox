#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);


    for (size_t i = 0; i <= 10; i++) {
        cout << i << ": " << bit_ceil(i) << "\n";
    }
}
